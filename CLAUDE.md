# AInovel — なろう系小説 AI自動執筆システム

このリポジトリは、なろう小説のヒット作をベースに「プロット・登場人物を登録 → 完成原稿を出力」するための Claude Code ネイティブの執筆システムです。Pythonスクリプトや外部APIキーは使いません。すべて Claude Code との対話（またはスキル）で完結します。

## ディレクトリ構成

```
novels/<作品ID>/          # 作品ごとに独立したフォルダ
  novel.json              # 基本情報（タイトル・主人公・文字数目安など）
  settings/
    world.md              # 世界観・勢力・地理・魔法体系
    characters.md         # キャラクター設定・口調・関係性
    plot_overview.md      # あらすじ・今後の展開・禁止事項・文体メモ
    continuity.md         # 整合性メモ（各話で確定した事実・伏線台帳）※自動更新
  knowledge_base/         # ベース作品の分析・執筆パターン
  episodes/               # 完成原稿（epNN_タイトル.md）
templates/                # 新作品登録用テンプレート
.claude/skills/           # スラッシュコマンド（new-novel / write-episode / analyze-base）
```

## 基本ルール

- **対象作品の特定**: `novels/` 配下に複数作品がある場合、ユーザーの指示から対象を判断する。曖昧なら確認する。1作品しかなければそれを使う。
- **設定が不完全でも書く**: settings の空欄・「未定」項目は、既存設定と矛盾しない範囲でその場で決めてよい。ただし**決めた内容は必ず該当する settings ファイルに追記して永続化する**こと（次回以降の矛盾を防ぐため）。
- **ユーザー入力はフレキシブルに受ける**: プロットは一行でも箇条書きでも長文でも受け付ける。足りない部分は plot_overview.md の「今後の展開方針」から補完する。

## 執筆パイプライン（エピソード執筆を頼まれたら必ずこの手順で）

スラッシュコマンドなしで「第X話を書いて」と頼まれた場合も、このパイプラインに従う。

1. **読み込み**: 対象作品の `novel.json`、`settings/` 全ファイル、`knowledge_base/` 全ファイル、直近のエピソード（`novel.json` の `recent_chapters_for_context` 話分、デフォルト5話）を読む。
2. **プロット確定**: ユーザー指定のプロットを基に、その話の起承転結を整理する。指定がなければ `plot_overview.md` から次の展開を提案し、そのまま執筆を続行する。
3. **執筆**: `knowledge_base/` の文体・パターン分析に従い、`episode_length_target`（±20%）の文字数で書く。
4. **自己チェック**: 下記チェックリストで検証し、問題があれば修正してから保存する。
   - [ ] 文字数が目標の±20%以内（`wc -m` で実測する）
   - [ ] 各キャラの口調が characters.md と一致
   - [ ] plot_overview.md の「禁止事項」に抵触していない
   - [ ] continuity.md・過去話の確定事実と矛盾していない
   - [ ] 話末がクリフハンガー or 次への引きで終わっている
   - [ ] 冒頭1〜3行で状況がつかめる（長い説明から始めない）
5. **保存**: `episodes/epNN_タイトル.md` として保存（既存話の命名規則に合わせる）。
6. **整合性メモ更新**: その話で新たに確定した事実・張った伏線・回収した伏線を `settings/continuity.md` に追記する。新キャラ・新地名・新能力が出たら characters.md / world.md にも反映する。
7. **報告**: 保存先・文字数・チェック結果・次話への引きを簡潔に報告する。

## スラッシュコマンド

| コマンド | 用途 |
|---------|------|
| `/new-novel` | 新作品の登録。ベース作品の情報を聞き取り、テンプレートから設定一式を生成 |
| `/write-episode` | 上記パイプラインでエピソードを執筆 |
| `/analyze-base` | ベースにしたいヒット作の本文を分析し、knowledge_base を生成・更新 |

## 文体に関する一般原則（全作品共通の下地）

- なろう読者向け: 1話3,500〜6,000字、会話比率40〜60%、話末は必ず引きで終える
- 冒頭は行動 or 会話から入る。説明から始めない
- 主人公の内心は `（　）` で表現（作品の文体設定が優先）
- 作品ごとの `knowledge_base/` がこの一般原則より優先される
